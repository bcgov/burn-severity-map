import { TagListProps as ReactAriaTagListProps } from "react-aria-components";
import { TagProps } from "../Tag";
import "./TagList.css";
export interface TagListProps<T> extends Pick<ReactAriaTagListProps<T>, "children" | "renderEmptyState"> {
    items: TagProps[];
    /**
     * Used to set the `flex-direction` on the TagList.
     */
    orientation?: "horizontal" | "vertical";
}
export default function TagList<T extends object>({ items, orientation, renderEmptyState, ...props }: TagListProps<T>): import("react/jsx-runtime").JSX.Element;
