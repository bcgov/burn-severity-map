import { DialogTrigger, DialogProps as ReactAriaDialogProps } from "react-aria-components";
import "./Dialog.css";
export interface DialogProps extends ReactAriaDialogProps {
    isCloseable?: boolean;
}
export interface DialogRenderProps {
    /** Handler function to close the Dialog */
    close: () => void;
}
export default function Dialog({ isCloseable, role, children, ...props }: DialogProps): import("react/jsx-runtime").JSX.Element;
export { DialogTrigger };
