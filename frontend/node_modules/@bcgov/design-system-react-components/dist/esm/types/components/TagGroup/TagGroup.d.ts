import { TagGroupProps as ReactAriaTagGroupProps } from "react-aria-components";
import "./TagGroup.css";
export interface TagGroupProps extends ReactAriaTagGroupProps {
    /**
     * Labels the TagGroup, appears above the tags.
     */
    label?: string;
    /**
     * Extended description of the TagGroup, appears below the tags.
     */
    description?: string;
    /**
     * Error message, appears below the tags and below the optional description.
     */
    errorMessage?: string;
}
export default function TagGroup({ children, label, description, errorMessage, ...props }: TagGroupProps): import("react/jsx-runtime").JSX.Element;
