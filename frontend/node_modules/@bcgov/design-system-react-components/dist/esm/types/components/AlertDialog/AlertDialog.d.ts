import { DialogTrigger, DialogProps as ReactAriaDialogProps } from "react-aria-components";
import "./AlertDialog.css";
export interface AlertDialogProps extends ReactAriaDialogProps {
    variant?: "info" | "confirmation" | "warning" | "error" | "destructive";
    title?: string;
    isIconHidden?: boolean;
    isCloseable?: boolean;
    buttons?: React.ReactNode;
}
export default function AlertDialog({ children, variant, role, title, isCloseable, isIconHidden, buttons, ...props }: AlertDialogProps): import("react/jsx-runtime").JSX.Element;
export { DialogTrigger };
